---
import './SectionContainer.scss';
import SectionHeading from './SectionHeading.astro';

interface SectionContainerProps {
    sectionNumber?: number;
    sectionTitle?: string;
    contentWidth?: boolean;
    className?: string;
    noTopMargin?: boolean;
    isStyled?: boolean;
}

const {
    sectionNumber = 1,
    sectionTitle = '',
    contentWidth = true,
    className = "",
    noTopMargin = false,
    isStyled = true,
}: SectionContainerProps = Astro.props;
---

<section class={`section-generic ${className} ${noTopMargin ? 'no-top-margin' : ''}`}>
    <div class={ contentWidth === true ? 'struct' : 'struct-width' }>
        {sectionTitle !== '' && (
            <SectionHeading isStyled={isStyled} headingType="h2" sectionNumber={sectionNumber}>{sectionTitle}</SectionHeading>
        )}
        <slot />
    </div>
</section>

